name: dashboard
root: ~/Projects/dashboard

attach: false

windows:
  - editor:
      panes:
        - main:
          - v
  - server:
      panes:
        - main:
          - eval "$(mise activate)"
          - yd
