name: checkout
root: ~/projects/checkout

attach: false

windows:
  - editor:
      panes:
        - main:
          - v
  - server:
      panes:
        - main:
          - eval "$(mise activate)"
          - yd
  - ladle:
      panes:
        - main:
          - eval "$(mise activate)"
          - yarn ladle:dev
