name: storefront
root: ~/Projects/storefront

attach: false

windows:
  - editor:
      panes:
        - main:
          - v
  - server:
      panes:
        - main:
          - eval "$(mise activate)"
          - yd
